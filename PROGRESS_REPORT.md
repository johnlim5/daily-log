# 先延ばし対策：実施報告

## 前提
あなた（別インスタンス）から以下のフィードバックを受け取り、実装に着手しました。

### 受け取ったフィードバックの要点
1. 先延ばしの原因は意志の弱さではなく、**外部強制力の不在・タスクの曖昧さ・締切のない生活**という構造
2. 朝のプログラム（daily-log）が成功したのは「トリガー → 具体的行動リスト → 完了確認」の3点セットが意志の代わりを果たしたから
3. これを**昼（13:00）と夜（22:00）にも展開**すべき
4. 進捗確認は**○△×方式**（文章を書く負荷を排除）

---

## 実施したこと

### 1. 達成度ダッシュボードの構築
既存のdaily-log（ https://johnlim5.github.io/daily-log/ ）に `/dashboard/` ページを追加しデプロイ済み。

**URL**: https://johnlim5.github.io/daily-log/dashboard/

**機能**:
- 今日の達成度を○△×で表示（朝・昼・夜の各カテゴリの完了状況つき）
- 週間カレンダー（月〜日の7日間に○△×を表示、過去週へのナビゲーション可能）
- 週間サマリー（○が7日中何日あったか）

**判定基準**（フィードバックに準拠）:
- 2〜3カテゴリ完了 = ○
- 1カテゴリ完了 = △
- 0カテゴリ = ×

### 2. メインアプリとの連携
daily-logのメイン画面右下にフローティングボタン（○）を追加。タップでダッシュボードに遷移。ダッシュボードからは「← 戻る」でメインに戻れる。

### 3. 通知について（設計判断）
- ブラウザ通知（Web Push / Notification API）はiOSではPWA化が必須で制約が多いため**採用しなかった**
- 代わりに **iPhoneの「ショートカット」アプリのオートメーション機能** を使い、13:00と22:00に音なしでdaily-logのURLを提示する方式を採用
- Apple Watchにも通知が届くため、手首でリマインダーを受け取れる
- これはアプリ側の改修不要で、ユーザーが端末側で設定するだけで完結する

---

## 技術的な補足
- daily-logのリポジトリにはビルド済みバンドル（Vite + React）のみが存在し、ソースコード（.tsx/.ts）は含まれていなかった
- そのため、既存のReactアプリを直接改修するのではなく、ポモドーロタイマー（/pomodoro/）と同じパターンで**独立したページ（vanilla JS）を追加**した
- 同じリモートAPI（Cloudflare Worker）と同じ認証（app_password）を共有しており、データは完全に同期している

---

## まだやっていないこと（次のステップ候補）

### ユーザー側の作業（アプリ外）
- [ ] iPhoneショートカットで13:00・22:00のオートメーション設定
- [ ] daily-logに**昼と夜のルーチンタスクを実際に登録**する（現在は朝のタスクのみ運用中の可能性あり）

### 1週間後の振り返りポイント
フィードバックの指示通り、以下を確認する：
- 3点セット（朝・昼・夜）が7日中何日できたか → ダッシュボードの週間カレンダーで自動集計済み
- **4日以上○なら成功**（完璧を目指さない基準）

### 将来的な拡張候補（必要に応じて）
- 連続記録（ストリーク）表示
- 月間カレンダー表示
- 「昼のチェックインまだです」のような時間帯別リマインド表示（アプリ内）

---

## 議論の過程で確認したこと
| 検討事項 | 結論 | 理由 |
|---|---|---|
| 別プログラムを作るか？ | No | データが同じ文脈に属する。開くアプリが増えると先延ばしリスクが増す |
| 既存ツール（Habitica等）を使うか？ | No | 朝のプログラムの成功パターンに最適化されたdaily-logを軸にすべき |
| daily-logをバージョンアップか？ | Yes | 追加機能は既存データの「見せ方」の問題であり、別ドメインの機能ではない |
| iPhone通知をアプリ側で実装するか？ | No | iOS制約が大きい。標準のショートカットアプリが最も確実 |
| Apple Watchで操作可能か？ | 通知受信のみ | ブラウザ操作は非実用的。通知のリレー装置として機能する |
